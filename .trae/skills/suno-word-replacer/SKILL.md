---\nname: "suno-word-replacer"\ndescription: "Replaces sensitive or unsuitable words in Suno prompts or lyrics. Invoke when creating, rewriting, or polishing Suno-related text."\n---\n\n# Suno Word Replacer\n\n在编写、改写或润色 Suno 相关提示词、歌词、标题、风格说明时，先执行以下常用字替换规则，以提升文本可用性与一致性。\n\n## 适用场景\n\n- 用户明确提到 `Suno`\n- 需要生成 Suno 歌词、提示词、歌曲描述\n- 需要对已有 Suno 文案做替换、清洗或规避性改写\n\n## 当前替换规则\n\n- `婴` 替换为 `因`\n- `岂` 替换为 `企`\n- `斋` 替换为 `摘`\n- `蒂` 替换为 `地`\n- `鉴` 替换为 `见`\n- `涵` 替换为 `含`\n- `谨` 替换为 `仅`\n- `蠹` 替换为 `度`\n- `弊` 替换为 `闭`\n- `斯` 替换为 `思`\n- `蹉` 替换为 `搓`\n- `跎` 替换为 `驼`\n- `稼` 替换为 `价`\n- `兮` 替换为 `西`\n- `驰` 替换为 `迟`\n- `柄` 替换为 `饼`\n- `倏` 替换为 `书`\n- `萦` 替换为 `盈`\n- `迤` 替换为 `以`\n- `逦` 替换为 `里`\n- `汲` 替换为 `及`\n- `彭` 替换为 `朋`\n- `稀` 替换为 `希`\n- `恬` 替换为 `甜`\n- `醍` 替换为 `提`\n- `醐` 替换为 `胡`\n- `昔` 替换为 `西`\n- `缄` 替换为 `坚`\n- `黜` 替换为 `处`\n- `卤` 替换为 `鲁`\n- `汞` 替换为 `拱`\n- `黍` 替换为 `鼠`\n- `姹` 替换为 `岔`\n- `掇` 替换为 `多`\n- `妇` 替换为 `富`\n- `榄` 替换为 `览`\n- `著` 替换为 `助`\n- `炁` 替换为 `气`\n- `蜇` 替换为 `哲`\n- `祛` 替换为 `区`\n- `斛` 替换为 `胡`\n- `炷` 替换为 `注`\n- `谒` 替换为 `叶`\n- `橐籥` 替换为 `驼越`\n- `圭` 替换为 `归`\n- `薰` 替换为 `勋`\n- `廛` 替换为 `缠`\n- `还丹` 替换为 `环丹`\n- `𬙎` 替换为 `卢`\n- `交媾` 替换为 `交构`\n- `调燮` 替换为 `调谢`\n- `枣` 替换为 `早`\n- `酉` 替换为 `有`\n- `还返` 替换为 `环返`\n- `去又还` 替换为 `去又环`\n- `膻` 替换为 `但`\n- `聒` 替换为 `锅`\n- `恼` 替换为 `脑`\n- `庐` 替换为 `卢`\n- `𬬻` 替换为 `卢`\n\n## 使用说明\n\n1. 先识别输入文本是否用于 Suno。\n2. 命中本技能适用场景时，优先应用替换规则。\n3. 替换后保持原句语义尽量自然，如有必要可顺带做轻微润色。\n4. 若后续用户继续补充新的替换词，直接在本技能中追加规则。\n\n## 示例\n\n原文：\n\n```text\n婴儿般纯净的声音\n```\n\n处理后：\n\n```text\n因儿般纯净的声音\n```\n